import React from 'react';
import './Header.css';

const Header: React.FC = () => {
    return (
        <header className="app-header">
            {/* 로고 및 타이틀 추후 구현 */}
        </header>
    );
};

export default Header;
